package manager;

   // public class IssueSort implements Comparator<Offer> {
     //   public int compare(Offer o1, Offer o2){
       //     return o1.getDuration() - o2.getDuration();
        //}
        //by timeOfAdding (newest, oldest)
        //by numberOfComments (least, most)
        //by numberOfReaction (most)
        //timeOfUpdate (recently, least recently)
//}
